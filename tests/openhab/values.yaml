persistence:
  userdata:
    existingClaim: openhab-userdata-pvc
  config:
    existingClaim: openhab-config-pvc

openhab:
  cloud_uuid: test
  cloud_secret: test
  user_id: 1000
  group_id: 1000
  timezone: Asia/Makassar

git:
  enabled: true
  repository: https://github.com/kilip/openhab
  envFrom:
    - secretRef:
        name: deploy-secrets
  persistence:
    existingclaim: openhab-pvc
