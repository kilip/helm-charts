imagePullSecrets:
  - name: github-secret
image:
  repository: ghcr.io/kilip/openhab
  tag: 3.4.3-rc1
  imagePullPolicy: Always
persistence:
  userdata:
    existingClaim: openhab-userdata-pvc
  config:
    existingClaim: openhab-config-pvc

openhab:
  cloud_uuid: test
  cloud_secret: test
  user_id: 1000
  group_id: 1000
  timezone: Asia/Makassar
